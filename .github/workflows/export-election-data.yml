name: Export Election Data

# DISABLED: Flutter 의존성(dart:ui)으로 인해 dart run 환경에서 실행 불가
# on:
#   schedule:
#     - cron: '*/10 * * * *'
#   workflow_dispatch:
#   push:
#     branches: [main]
on:
  workflow_dispatch:

jobs:
  export-election-data:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled - Flutter dependency not available in dart run"
